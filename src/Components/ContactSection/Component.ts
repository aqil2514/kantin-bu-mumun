import { Box, styled } from "@mui/material";

export const ContactContainer = styled(Box)({
  background: "linear-gradient(180deg, #a9ec76, #fff)",
  width: "100%",
  padding: "2rem",
  minHeight: "100vh",
});
